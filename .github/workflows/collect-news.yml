name: V5 Collect News

  on:
    schedule:
  - cron: "*/30 * * * *"
workflow_dispatch:

  permissions:
    contents: write

  jobs:
    run:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout repository
          uses: actions/checkout@v4
        - name: Setup Node.js
          uses: actions/setup-node@v4
          with:
            node-version: "20"
        - name: Run task
          run: node scripts/collect-news.js && node scripts/incident-engine.js && node scripts/cleanup-data.js
        - name: Commit generated data
          uses: stefanzweifel/git-auto-commit-action@v5
          with:
            commit_message: "V5 Collect News"
            file_pattern: |
              data/*.json
